# terraform-aws-STARTUP

1. Using GCP I will create AWS resources in terraform.
2. Setup aws configuration: run "aws configure" or check them "aws sts get-caller-identity"
3. AWS EC2 with t3.micro on public subnet.
4. VPC has public subnet with security group port 22,80,443 are open. Configured Internet Gateway.
5. 